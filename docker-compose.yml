services:
  db:
    image: postgres:16-alpine
    container_name: house_management_db
    environment:
      - POSTGRES_USER=ishibashi
      - POSTGRES_PASSWORD=p@ssword123
      - POSTGRES_DB=HouseManagementDB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: